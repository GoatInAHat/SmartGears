;;; topology-kinematics.lsp -- Propagate RPM/direction through gear graphs (stub).
;;; Purpose: signed ratios, compound axles, loop consistency checks.

(defun sg-kin-propagate (graph driver-id rpm)
  "Stub: Will later propagate RPM from DRIVER-ID through GRAPH."
  *sg-placeholder*)

(defun sg-kin-compose-ratio (mesh)
  "Stub: Will later compute signed ratio for a mesh edge."
  *sg-placeholder*)

(princ)
